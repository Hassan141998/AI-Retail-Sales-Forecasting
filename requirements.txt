# ============================================================
# Vercel deployment requirements — lightweight API only
# TensorFlow/LSTM excluded (too large for serverless 250MB limit)
# For local full training use: pip install -r requirements.local.txt
# ============================================================

# Core
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2

# Time Series (ARIMA)
scipy==1.11.4
statsmodels==0.14.1

# Web API
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
python-dotenv==1.0.1
httpx==0.27.0

# Database (Neon PostgreSQL)
psycopg2-binary==2.9.9
sqlalchemy==2.0.30

# Utilities
loguru==0.7.2
joblib==1.4.2
